"use strict";  // treat all JS code in a newer version but its not needed for now because automataion

//alert(3 + 4) it will only usable in brower's js engine but if i want to do the same in node.js then the syntax is slidly changed


 let roll = 12;
 let name = "Hemel";
 let bool = Boolean;
 let state; // undefined
 let value = null;

//  undefined and null is not same ,null means there is value 0

/*
null usecase:
suppose the sever needs to give me No value temperature , so if it gives me 0 || confusion can be arrised as 
temperature 0 is exists || so then it will give m e null (mane kisu dibo na )
*/


// question for interview 
/*
the datatype of null -> object
the datatype of undefined -> undefined 
*/

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object

console.log(typeof(10));
console.log(typeof 10);